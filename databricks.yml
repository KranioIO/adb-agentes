bundle:
  name: projet-test-2

variables:
  target_catalog:
    default: main_dev

sync:
  include:
    - datayanalytics/** # Solo incluye esta carpeta y todo su contenido
  exclude:
    - .git/** # No queremos el historial de git en el workspace
    - .vscode/** # No queremos archivos del editor
    - "**/*.pyc"          # No queremos archivos basura de Python
    - .databricks/** # Excluye metadatos locales del CLI

targets:
  dev:
    mode: production
    default: true
    workspace:
      host: https://adb-1194260751860413.13.azuredatabricks.net
      root_path: /Shared/${bundle.name}/${bundle.target}
    run_as:
      service_principal_name: "a963eeab-b3eb-44fc-87c5-8365bf6a0a42"

  qa:   
    mode: production
    workspace:
      host: https://adb-1832665218446849.9.azuredatabricks.net
      root_path: /Shared/${bundle.name}/${bundle.target}
    variables:
      target_catalog: main_qa
      
